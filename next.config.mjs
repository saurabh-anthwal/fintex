/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn-icons-png.flaticon.com','cryptologos.cc','png.pngtree.com'], // Add the external domain here
    },
  };
  
  export default nextConfig;
